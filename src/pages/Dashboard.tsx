// RAEVOLT merchant dashboard entry — protected shell with nested routes.
import DashboardLayout from "@/components/DashboardLayout";

export default function Dashboard() {
  return <DashboardLayout />;
}
